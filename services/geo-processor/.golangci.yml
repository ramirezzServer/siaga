version: "2"
# misspell sengaja tidak dipakai: komentar dan pesan ditulis dalam bahasa Indonesia.
linters:
  default: standard
  enable:
    - errorlint
    - gosec
    - nolintlint
    - revive
    - unconvert
    - unparam
    - bodyclose
    - noctx
    - errname
    - exhaustive
  settings:
    revive:
      rules:
        - name: exported
          arguments: [disableStutteringCheck]
  exclusions:
    presets: [std-error-handling]
    rules:
      # Property test sengaja memakai RNG berbenih agar kasus gagal bisa diulang persis.
      - path: _test\.go
        linters: [gosec]
        text: G404
formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    goimports:
      local-prefixes: [github.com/ramirezzServer/siaga]
