{
  "name": "@siaga/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Tipe event SIAGA hasil generate dari contracts/proto (protobuf-es).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@bufbuild/protobuf": "2.15.0"
  },
  "devDependencies": {
    "vitest": "5.0.1"
  }
}
